import styled from "styled-components";
import Navbar from "components/navbar";

const Home = () => {
  return (
    <Container>
      <Navbar />
    </Container>
  );
};

const Container = styled.div``;

export default Home;
